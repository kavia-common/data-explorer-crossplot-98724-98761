# Example environment variables for the Crossplot Frontend
# Rename this file to ".env" and set the API URL.
# In Create React App, environment variables must be prefixed with REACT_APP_
REACT_APP_API_URL=https://your-api.example.com/data
